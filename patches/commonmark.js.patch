diff --git a/lib/cjk-ranges.js b/lib/cjk-ranges.js
new file mode 100644
index 0000000..e664e46
--- /dev/null
+++ b/lib/cjk-ranges.js
@@ -0,0 +1,93 @@
+"use strict";
+
+// Generated by scripts/generate-unicode-tables.
+// Unicode 17.0.0. Do not edit by hand.
+var cjkRanges = [
+    [0x1100, 0x11FF],
+    [0x20A9, 0x20A9],
+    [0x2329, 0x232A],
+    [0x2630, 0x2637],
+    [0x268A, 0x268F],
+    [0x2E80, 0x2E99],
+    [0x2E9B, 0x2EF3],
+    [0x2F00, 0x2FD5],
+    [0x2FF0, 0x303E],
+    [0x3041, 0x3096],
+    [0x3099, 0x30FF],
+    [0x3105, 0x312F],
+    [0x3131, 0x318E],
+    [0x3190, 0x31E5],
+    [0x31EF, 0x321E],
+    [0x3220, 0x3247],
+    [0x3250, 0xA48C],
+    [0xA490, 0xA4C6],
+    [0xA960, 0xA97C],
+    [0xAC00, 0xD7A3],
+    [0xD7B0, 0xD7C6],
+    [0xD7CB, 0xD7FB],
+    [0xF900, 0xFA6D],
+    [0xFA70, 0xFAD9],
+    [0xFE10, 0xFE19],
+    [0xFE30, 0xFE52],
+    [0xFE54, 0xFE66],
+    [0xFE68, 0xFE6B],
+    [0xFF01, 0xFFBE],
+    [0xFFC2, 0xFFC7],
+    [0xFFCA, 0xFFCF],
+    [0xFFD2, 0xFFD7],
+    [0xFFDA, 0xFFDC],
+    [0xFFE0, 0xFFE6],
+    [0xFFE8, 0xFFEE],
+    [0x16FE0, 0x16FE4],
+    [0x16FF0, 0x16FF6],
+    [0x17000, 0x18CD5],
+    [0x18CFF, 0x18D1E],
+    [0x18D80, 0x18DF2],
+    [0x1AFF0, 0x1AFF3],
+    [0x1AFF5, 0x1AFFB],
+    [0x1AFFD, 0x1AFFE],
+    [0x1B000, 0x1B122],
+    [0x1B132, 0x1B132],
+    [0x1B150, 0x1B152],
+    [0x1B155, 0x1B155],
+    [0x1B164, 0x1B167],
+    [0x1B170, 0x1B2FB],
+    [0x1D300, 0x1D356],
+    [0x1D360, 0x1D376],
+    [0x1F200, 0x1F200],
+    [0x1F202, 0x1F202],
+    [0x1F210, 0x1F219],
+    [0x1F21B, 0x1F22E],
+    [0x1F230, 0x1F231],
+    [0x1F237, 0x1F237],
+    [0x1F23B, 0x1F23B],
+    [0x1F240, 0x1F248],
+    [0x1F260, 0x1F265],
+    [0x20000, 0x2A6DF],
+    [0x2A700, 0x2B81D],
+    [0x2B820, 0x2CEAD],
+    [0x2CEB0, 0x2EBE0],
+    [0x2EBF0, 0x2EE5D],
+    [0x2F800, 0x2FA1D],
+    [0x30000, 0x3134A],
+    [0x31350, 0x33479],
+];
+
+var isCjk = function(codePoint) {
+    var low = 0;
+    var high = cjkRanges.length - 1;
+    while (low <= high) {
+        var middle = Math.floor((low + high) / 2);
+        var range = cjkRanges[middle];
+        if (codePoint < range[0]) {
+            high = middle - 1;
+        } else if (codePoint > range[1]) {
+            low = middle + 1;
+        } else {
+            return true;
+        }
+    }
+    return false;
+};
+
+export default isCjk;
diff --git a/lib/inlines.js b/lib/inlines.js
index d2907b7..1ba122b 100644
--- a/lib/inlines.js
+++ b/lib/inlines.js
@@ -3,6 +3,7 @@
 import Node from "./node.js";
 import * as common from "./common.js";
 import fromCodePoint from "./from-code-point.js";
+import isCjk from "./cjk-ranges.js";
 import { decodeHTMLStrict } from "entities";
 
 var normalizeURI = common.normalizeURI;
@@ -255,7 +256,8 @@ var scanDelims = function(cc) {
     var after_is_whitespace,
         after_is_punctuation,
         before_is_whitespace,
-        before_is_punctuation;
+        before_is_punctuation,
+        adjacent_cjk;
 
     if (cc === C_SINGLEQUOTE || cc === C_DOUBLEQUOTE) {
         numdelims++;
@@ -284,15 +286,22 @@ var scanDelims = function(cc) {
     after_is_punctuation = rePunctuation.test(char_after);
     before_is_whitespace = reUnicodeWhitespaceChar.test(char_before);
     before_is_punctuation = rePunctuation.test(char_before);
+    adjacent_cjk =
+        (cc === C_ASTERISK || cc === C_UNDERSCORE) &&
+        (isCjk(char_before.codePointAt(0)) || isCjk(cc_after));
 
     left_flanking =
         !after_is_whitespace &&
         (!after_is_punctuation ||
             before_is_whitespace ||
-            before_is_punctuation);
+            before_is_punctuation ||
+            adjacent_cjk);
     right_flanking =
         !before_is_whitespace &&
-        (!before_is_punctuation || after_is_whitespace || after_is_punctuation);
+        (!before_is_punctuation ||
+            after_is_whitespace ||
+            after_is_punctuation ||
+            adjacent_cjk);
     if (cc === C_UNDERSCORE) {
         can_open = left_flanking && (!right_flanking || before_is_punctuation);
         can_close = right_flanking && (!left_flanking || after_is_punctuation);
diff --git a/test/regression.txt b/test/regression.txt
index a3f2d7d..d40f6b3 100644
--- a/test/regression.txt
+++ b/test/regression.txt
@@ -60,7 +60,7 @@ Issue #108 - Chinese punctuation not recognized
 ```````````````````````````````` example
 **。**话
 .
-<p>**。**话</p>
+<p><strong>。</strong>话</p>
 ````````````````````````````````
 
 Issue jgm/cmark#177 - incorrect emphasis parsing
@@ -570,3 +570,40 @@ a**𝜵a**a
 .
 <p><img src="asdf.gif" alt="asdf  asdf" /></p>
 ````````````````````````````````
+
+CJK characters and punctuation participate in emphasis flanking.
+
+```````````````````````````````` example
+これは**「重要なこと」**です。
+
+語*「引用」*語
+
+**안녕(hello)**하세요.
+
+𠮷**(U+20BB7)**語
+
+a**「x」**b
+.
+<p>これは<strong>「重要なこと」</strong>です。</p>
+<p>語<em>「引用」</em>語</p>
+<p><strong>안녕(hello)</strong>하세요.</p>
+<p>𠮷<strong>(U+20BB7)</strong>語</p>
+<p>a<strong>「x」</strong>b</p>
+````````````````````````````````
+
+Whitespace, underscores, emoji, and variation selectors keep their existing behavior.
+
+```````````````````````````````` example
+語__「強調」__語
+
+語** 強調**語
+
+a**😀。**b
+
+禰󠄀**(ね)**豆子
+.
+<p>語__「強調」__語</p>
+<p>語** 強調**語</p>
+<p>a**😀。**b</p>
+<p>禰󠄀**(ね)**豆子</p>
+````````````````````````````````
diff --git a/test/smart_punct.txt b/test/smart_punct.txt
index 3522c94..b99bae9 100644
--- a/test/smart_punct.txt
+++ b/test/smart_punct.txt
@@ -166,3 +166,11 @@ No ellipses\.\.\.
 .
 <p>No ellipses...</p>
 ````````````````````````````````
+
+CJK-aware emphasis flanking does not change smart quotes.
+
+```````````````````````````````` example
+「"quote"」
+.
+<p>「“quote”」</p>
+````````````````````````````````
